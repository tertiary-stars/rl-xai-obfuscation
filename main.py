# This file is now deprecated. Please use `train_agent.py`.
# The classes `Adversary` and `XAIObfuscationEnv` have been moved to
# `src/adversary.py` and `src/environment.py` respectively.
# The function `load_and_train_target` has been moved to `src/utils.py`.
#
# This change was made to improve project structure and modularity.
# The training logic is now cleanly separated in `train_agent.py`.

print("This file (`main.py`) is deprecated. Please run `train_agent.py` to train the agent.")